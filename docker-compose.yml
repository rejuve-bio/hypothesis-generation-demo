version: '3.8'

services:
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - app_network

  prolog-service:
    build:
      context: .
      dockerfile: Dockerfile.prolog
    ports:
      - "4242:4242"
    volumes:
      - ./pl:/app/pl
      - /mnt/d2_nfs/wondwossen/prolog_out_v2:/app/mnt/d2_nfs/wondwossen/prolog_out_v2
      - /mnt/d2_nfs/wondwossen/prolog_out_v3:/app/mnt/d2_nfs/wondwossen/prolog_out_v3
    networks:
      - app_network

  flask-app:
    build: .
    ports:
      - "5005:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    depends_on:
      - mongo
      - prolog-service
      - prefect-service
    command: >
      sh -c "
      python main.py --ensembl-hgnc-map data/ensembl_to_hgnc.pkl --hgnc-ensembl-map data/hgnc_to_ensembl.pkl \
      --go-map data/go_map.pkl --swipl-host prolog-service --swipl-port 4242 
      "
    networks:
      - app_network

  prefect-service:
    build:
      context: .
      dockerfile: Dockerfile.prefect
    ports:
      - "4200:4200"
    volumes:
      - prefect-data:/root/.prefect 
    environment:
      - PREFECT_SERVER_PORT=4200
    command: prefect server start
    networks:
      - app_network

volumes:
  mongo-data:
  prefect-data:

networks:
  app_network:
    driver: bridge
